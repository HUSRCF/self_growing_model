# 直接能量评分拟合输出核：训练改善，完整数值门槛仍失败

2026-09-18。`fit_energy_output_kernel.py`，`test_energy_output_kernel.py`，结果 `adaptive_search_results/energy_output_kernel_fit.json`。

冻结原F/GRU及预测中心。旧80 TRAIN窗口481017/P32动作482017，旧NLL逐位回放；没有读取hold/DEV/TEST。三时距分别拟合两角浓度，共6参数，1025网格零锚定U均值，单起点log10、边界1e-4..1e6、L-BFGS-B最多200步。按窗口平均充分统计量，聚合值/梯度与批量均值单测一致；均值优化值亦对逐窗复算atol1e-13一致。

|时距|两角kappa|零核TRAIN U|1025拟合U|2049复核相对零变化|
|---|---|---:|---:|---:|
|0.5s|1319.602,146.437|.14681063|.14299305|−.00381055|
|1s|258.130,17.617|.41448694|.40814889|−.00633767|
|3s|9.396,2.057|.84054699|.83504888|−.00549770|

三组优化均收敛、无参数触界，迭代13/12/8。均为拟合内结果，不能当泛化改善。边界/起点沿用原NLL协议，但样本中心为原32而非后来128；不把与128核比较归因于唯一目标差异。零核仍是原始对照，未据训练分数替换模型。

全部拟合冻结后逐一检查80窗口1025→2049，原评分门槛1e-4、梯度1e-5不变：

- 最大评分差 .000120280253，**失败**；分时距 .00012028/.0000680344/.0000206653。
- 最大梯度差 .00000108074，通过。
- 最差为video4/start1684/0.5s，不能剔除或放宽阈值。

此前10个案例通过不能保证80窗口或新浓度都通过。没有按2049重新优化，没有证明整个优化路径积分精度；只复核最终候选。评分差仍比平均训练改善小，但不能以此覆写预设逐窗门槛。

拟合/统计预计算CPU8.35秒，完整逐窗分辨率审计188.88秒（不含原始rollout），说明本轮主要开销在验证而不是2参数优化。进程一次RSS约932MB，不是峰值。161单测通过，F/GRU SHA未变，guard0；CPU单线程，无GPU训练。

下一步先固定最差窗口（诊断选择，不能作为独立验证集），拆分raw平滑积分与零核锚定，必要时单窗口4097检查；原80窗失败保持。然后建立可用于完整候选评估的精度方案，再冻结协议做独立动作随机性/留出评估。不得仅将检查缩回通过的10窗口或只比较总体均值。
