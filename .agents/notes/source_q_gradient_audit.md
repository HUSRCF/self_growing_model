# 截断 q 条件梯度：固定状态重复采样

2026-09-17。`audit_source_q_gradient.py`、`test_source_q_gradient.py`，报告 `adaptive_search_results/source_q_gradient_audit.json`。

## 协议

固定theta0、拟合窗口seed190101（10TRAIN拟合视频各1前缀窗口），P4、300步、50步history/hidden detach。8新动作seed371017–371024，各窗口j加j*100000。逐窗口记录16维混合梯度、路径梯度、源q访问量；共享梯度使用已验证的sum_q链式关系还原，不另换轨迹。

没有裁剪、更新、训练留出、DEV或TEST。访问量为相关的粒子步，不当独立样本数；统计重复单位是固定窗口下的动作seed。

## 方向复现性

| 估计器 | 均值范数/RMS标准误 | 成对余弦均值 | 相反方向对数 | 前四/后四均值余弦 |
|---|---:|---:|---:|---:|
|q表混合梯度|.6211|−.07275|16/28|−.60143|
|共享求和梯度|.40295|−.06087|15/28|−.80921|
|仅路径项（有偏对照）|1.7549|.46342|7/28|.47521|

这里RMS标准误=sqrt(样本协方差迹/8)，比值只是描述性量，不是显著性检验或总体信噪比证明。八个seed不足以断言真实期望为零，也不能外推到所有参数/窗口。

q表混合梯度协方差迹.00971934，路径项8.36536e-5，相差约116倍。说明当前完整评分项组合中的波动值得重点处理；但省略评分项会改变期望，不能只因路径项稳定就称其正确。

## 访问量不是唯一问题

八组共96000源状态粒子步，各q计数：[18676,15168,19603,9928,14699,3649,9342,4935]。所有q都有访问。访问最多的q2，均值/RMS-SE=.47755，成对余弦−.14285，16/28方向相反；因此不能仅归因为某些q没有被采到。最少访问q5计数3649也不构成3649个独立样本。

所有梯度有限，守卫触发0。132项测试通过，原F/GRU SHA不变。新增测试核对余弦、尺度不变性、协方差缩放、零向量及相反均值情形。

## 下一步

暂停继续增加条件参数。优先验证评分梯度控制变量：在独立样本上估计一个标量基线校正，利用总logprob梯度期望为零来降低现有混合估计器方差，保留原LOO项。先在可穷举toy验证期望保持与有限样本拟合泄漏风险，再做训练seed拟合／不同seed评估的对照，不能在同一批样本上拟合并宣称无偏降噪。

它至多保持现有截断估计器的期望，不能修复50步截断本身对原完整目标的偏差；没有实测收益前不开展新模型推广。该控制变量尚未实现。
