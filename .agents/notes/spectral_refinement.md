# 输出核细网格：固定案例达到原收敛门槛

2026-09-18。脚本 `audit_spectral_refinement.py`；报告 `adaptive_search_results/spectral_refinement.json`。

固定上一轮10个TRAIN视频各首窗、原80窗口完整482017/P32随机续跑后选择同10窗、旧32/new128两组核。每次只构造一个窗口/时距的统计量，限制工作内存。257分块结果对旧批量cost/raw/gradient以atol1e-13、rtol0重放通过；不是逐位一致要求。零核仍逐位退化，raw与锚定误差恒等式通过。

先实施257/513/1025，发现锚定评分仍失败后追加2049，重跑前三档结果相同。追加是诊断驱动，不宣称四档一次性预注册；原评分1e-4/梯度1e-5门槛未改变，历史失败完整保留。

|比较|new128锚定评分最大差|raw评分最大差|梯度最大差|锚定评分/梯度|
|---|---:|---:|---:|---|
|257→513|8.79995e-4|4.88777e-5|2.40708e-5|失败/失败|
|513→1025|1.49525e-4|6.10661e-6|2.96254e-6|失败/通过|
|1025→2049|6.81866e-5|7.64453e-7|3.69567e-7|通过/通过|

old32最后一级分别6.82285e-5、6.93756e-7、3.41232e-7，也全部通过。两组核raw与梯度随分辨率改善；锚定评分更慢，符合原无核谱近似误差占主要贡献的诊断，但不证明唯一误差来源或普适收敛阶。

最后运行各网格CPU约 .229/.907/4.761/24.490秒，包括30个窗口-时距预计算及两组核评价。2049进程一次RSS观测约944MB，不称峰值；不是纯训练每步成本，也不是等精度速度对照。继续采用单线程，未用120线程或GPU。

160项完整unittest通过；F/GRU SHA未变；真实轨迹守卫0。没有训练、hold、DEV、TEST或默认模型变更。

结论限于两个冻结核、10案例的相邻网格收敛，不是严格绝对误差界，不覆盖优化可能到达的任意浓度。接下来可设计TRAIN U-energy宽度拟合：每时距零噪声候选保留，冻结所有协议后实施；需限制/检测新的浓度对应的数值分辨率并对最终候选复核。为提速可研究将高分辨率距离谱和低频核统计分开，但必须先验证截断误差，不能直接沿用旧低分辨率距离FFT。尚未实施新训练。
