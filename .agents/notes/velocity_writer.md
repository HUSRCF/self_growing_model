# 同容量速度条件化writer对照

2026-09-17。`train_closed_loop_writer.py --writer velocity`；`validate_velocity_writer.py`。

## 预定协议

保留两参数、原bound=[4.42047e-5,7.86352e-5]rad、P4×12轮直接闭环搜索、三训练种子、相同TRAIN窗口/动作随机数/留出选择协议。仅将恒定delta=value改成delta=value*tanh((h[-1]−h[-2])/velocity_scale)。当前一步角增量为0时校正为0；符号可正可负，不预设纯阻尼，更不是物理定律。

velocity_scale只取10个TRAIN拟合视频300步可用池当前增量绝对值中位数，得到[.09809469444596175,.19077136638273373]rad/帧；不是rad/s。使用前后相邻过去帧，不读取未来目标或留出视频。相同最大幅度不意味着相同有效RMS，属于模型形式改变的一部分。

所有3种子保留，选中epoch1901/2718/3141分别8/4/8，选模型目标.431003/.415813/.422155 vs零.438965。参数符号不一致：1901[-.076007,.124860]，2718[-.148001,.005259]，3141[.497284,−.210439]，不能声称学到了共同物理阻尼。

验证固定constant/velocity两个家族共6模型、零模型，24个TRAIN留出窗口P8、seed111017–111020。核验训练窗口/预算/边界一致、epoch0结果精确相同、归一化三种子相同。模型SHA不变；零参数复用同次rollout，不冒充独立观察。家族平均是模型性能均值，不是预测集成。无DEV/TEST，无正式检查器执行。

78测试通过，新增静止归零、奇对称/限幅/角平移不变性、归一化只读取当前与过去、零校正精确重放和未来真值隔离。

## 新随机种子结果

零校正目标.4355500；constant家族.4327577，差−.0027923/条件RNG SE .0027428；velocity家族.4419941，差+.0064441/SE .0068685。velocity训练种子1901/2718/3141各自均值差+.0078838/+.0035636/+.0078849，全部不支持改善；但有限4动作种子不证明可靠有害。

RMSE(.5/1/3秒)：zero .126516/.364576/.658532；constant .124672/.363298/.656318；velocity .126191/.364629/.673567。velocity短时基本持平，长时变差。全部数值失败0不是正式约束合法声明。去重后20次实际复核rollout，文件SHA未变。

常数模型本次弱改善与此前若干新种子的恶化方向相反，不能挑本轮结果推翻此前结论。两参数速度条件化仍未获得可复现优势，不进入DEV/TEST、不推广。新增writer分支后原constant默认seed1901再次完整重放，trace/检查点/模型仍精确一致。

下一步优先审计少量随机评估的检查点选择：固定已有0/4/8/12参数，在更多独立动作种子上比较排序与原选择，不重训、不以审计真值选部署赢家。目标是区分训练候选质量与模型选择噪声，避免继续盲目换结构。两参数新结构失败不能推出全部速度建模或连续分布失败。
