# 初始化器训练覆盖32→256起点/视频

2026-09-17。沿用`multistep_initializer_pilot.py --per-video 256`及对应hold审计。新增`adaptive_search_results/multistep_initializer_n256_pilot.json`和`multistep_initializer_n256_hold_audit.json`；旧32默认输出保留，未覆盖旧报告/模型。

## 不变项与限制

10TRAINprefix，h1/h10训练起点数同时320→2560，均匀线性网格。旧scale、137特征、ridge每样本强度.001、6轮反求、±.02残差限幅均不变。32/256网格非严格嵌套，邻近窗口重叠；不是8倍独立样本或新增视频，也不是等训练计算预算。两组均只使用观测q参考路径生成标签。

旧32模型通过只读重建，h1/h10回归coef与旧NPZ精确相等。扩大覆盖评估的原F/原初始化预测和失败标记与旧201017–201020轨迹精确相等。零残差完整回放不变，105项测试通过。原源模型SHA未变。无DEV/TEST、无新超参搜索或默认升级。

## 参考路径拟合/留出

2560起点的h10训练参考误差5.63487e-5→4.62284e-5（因果回归后）；逐窗口反求可到3.12028e-6，限幅命中0。h1训练4.66173e-6→3.84954e-6，反求目标可到0。新增覆盖的回归拟合误差比320时高，不能单独视为坏事。

同24个hold起点，前10步固定参考路径MSE：

| 初始化器 | 32起点/视频训练 | 256起点/视频训练 |
|---|---:|---:|
| 原初始化 | 6.83653e-5 | 6.83653e-5 |
| h1 | 2.16933e-4 | 7.57054e-5 |
| h10 | 8.67930e-5 | **5.00221e-5** |

h10新增覆盖后比原初始化低26.83%，且三个hold视频前10步均改善；原320协议比原初始化更差。结果支持覆盖/回归泛化问题的解释，但不能证明唯一原因是样本数量。

## 正常事件抽样生成

仍是TRAINhold24/P8/300、既有seed201017–201020，配对筛查而非新随机种子验证。综合U-energy均值：

| 模型 | 32起点/视频 | 256起点/视频 |
|---|---:|---:|
| 原F | .449963 | .449963 |
| 原初始化积分 | .486340 | .486340 |
| h1积分 | .506357 | .481469 |
| h10积分 | .514222 | .474402 |

新增h10−原初始化四种子[-.021623,-.012542,+.007735,-.021320]，均值-.011938（约-2.45%）；3/4改善。仍比原F综合高约5.43%，不能称整个系统升级。

256版原初始化/h1/h10的.5/1/3秒嵌入RMSE分别：

- 原初始化：.165459/.437982/.649364。
- h1：.168884/.415943/.647925。
- h10：.162197/.436980/.624589。

全部数值失败0。h10主要长期收益，原F短期优势仍大。训练CPU h1约1.275秒/h10约11.427秒，相对旧约.118/1.077秒，受批量形状/内存与本次计时影响，不把这些作为严格扩展性基准。

## 下一步（未执行）

冻结256版h10，并同时保留h1（不丢掉对照），用预定新动作随机种子复核正常生成，对照原F和原初始化。不要进一步扫覆盖数、岭强度或horizon；若新随机种子没有保持收益，则停止将当前筛查均值当作改进证据。此前hold已重复使用，任何结果都不是独立视频泛化结论。
