# 冻结U核完整80窗精度通过

2026-09-18。`audit_energy_kernel_refinement.py`；`adaptive_search_results/energy_kernel_refinement.json`。

原80TRAIN窗口481017/P32动作482017、上轮3组U拟合核完全冻结。并未只挑最差窗口检查，而是全部80×3时距在2049/4097重算。旧1025→2049评分失败仍保留，原门槛score1e-4/gradient1e-5没有放宽；没有重训/筛选窗口。

## 实现

`spectral_kernel_energy.blocked_value`把全频率矩阵按128行分块，特征矩阵用复数矩阵乘积计算，累加raw平滑评分、无核谱基准、梯度。没有删掉高频或改变零锚定公式。`moment_factors`仅提取原Bessel比值计算，不改变表达式。

块大小1/7/33/80对旧批量实现值/梯度、含失败惩罚、零核精确退化单测通过。真实2049全部80窗对上轮保存cost/gradient以atol1e-13、rtol0复现。162项全量单测通过。

## 结果

2049→4097全80窗口最大变化：

- 锚定评分2.18798357e-5，PASS。
- 原始平滑评分2.76416981e-7。
- 无核谱基准误差变化2.19154519e-5。
- 梯度1.34658221e-7，PASS。

锚定评分最差video1/start978/1秒，未剔除。4097无核谱基准对精确点分数的最大误差8.86338e-6。原始积分的网格变化明显小于锚定评分，支持主要变化来自零核谱修正，不意味着核积分绝对误差已经严格定界。

4097三时距平均训练改变量−.0038097567/−.0063378095/−.0054977445，原改善保持；仍是拟合内证据。检查只覆盖最终候选与这些窗口，不能声称整个优化路径、任意核浓度或其他数据都通过。

2049/4097完整计算CPU36.43/137.69秒；进程一次RSS约958MB，不是峰值。旧实现audit188.88秒含1025与2049且工作内容不同，不能直接称严格等任务加速倍数；本次2049仍复现旧结果。CPU单线程，F/GRU SHA未变，守卫0，无hold/DEV/TEST/推广。

## 下一步

冻结U拟合核与原NLL32核（同样32拟合中心）及零核，预先固定新动作随机种子，在原TRAINhold视频18/16/13前缀进行配对检验。需要保留逐视频/时距结果，旧留出多次复用，不称全项目盲测；不根据留出再调核宽或挑时距。可以使用确定性核积分消除附加核噪声，但必须为新窗口复核分辨率，不能直接假定此次通过可泛化。此项尚未执行。
