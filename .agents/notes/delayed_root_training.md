# 延后一次目的档干预：前50步精确保护，新训练方向为r6

2026-09-18。扫描仓库相关实现/记忆，没有找到等价的在第51个生成点干预一次的实验；既有“延后一拍”是checker时序，不等同此机制。

`audit_crossfit_value.continuation`新增`forced_step=0`，合法整数0..299，原默认不变。新`audit_delayed_root.py`与`test_delayed_root.py`，报告`delayed_root_training.json`。

## 时序和分布语义

原80TRAIN根状态，先由原模型生成50步，保持q、GRU hidden和历史，不重置记忆；在zero-based50（第51个生成点）强制目的r一次，后续恢复原模型。事件与r的两组uniform仍照常抽取，确保共同随机流配对。守卫逻辑不变。

九个分量：完全原始轨迹，加八个强制目的r的轨迹。策略为(1-alpha)原分量+alpha强制r分量，alpha<=.25。这里是每个独立粒子的分量混合，不是在预测起点用p(r)给后续强制分量加权，也不是固定一部分粒子替换后误当iid集合。完整U分量求和仍排除跨分量相同粒子索引。

先完整复现历史451017/P4八个起点强制根的A/B及guard，逐位一致，保护API默认语义。然后新761017–761020四流/P8每分量、300步，4CPU进程BLAS1。每个强制分量的前50步完整轨迹和失败标记逐位等于配对原始分量，四流0.5秒L/Q原始抵消误差也是0。代码只在先验证<=1e-14后将结构性短期零系数写为精确0。

## 新训练曲线

全部八个方向/alpha优化结果保留，零可选，仅训练评分，不是新视频性能。

|r|训练最优alpha|综合Δ|
|---|---:|---:|
|0|.115510242|−.000090920|
|1|.183267106|−.000169730|
|2|.25|−.000271897|
|3|.199313052|−.000154096|
|4|0|0|
|5|.195268659|−.000300703|
|6|.25|−.003405420|
|7|.25|−.001638294|

训练最优r6/.25，0.5/1/3秒Δ=0/−.005966043192882407/−.004250218120059589。均值按三时距统计；由于短期恒零，最小化与两个长时距均值等价。r6与起点干预的r5/r7不同，不能搬用旧根权重。

217项测试通过，所有guard0、源hash不变。新测试覆盖延后首50步相等、干预首次影响第51点、无干预全轨迹一致、默认首点干预及参数校验。无CV、晚段/hold/DEV/TEST或默认推广。

下一步按视频排除用这批新标签拟合r/alpha，冻结后新动作流评价排除视频，保留原分量及预定固定r6/.25控制（全量训练选出，须标注不是LOVO无泄漏学习控制）。重点核对1秒/3秒是否都迁移，短期保持结构性零。不要扫干预时刻或沿用起点critic。尚未实施。
