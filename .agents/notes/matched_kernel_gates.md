# 门控匹配新核：训练改善，时间迁移反而退化

2026-09-18；脚本 `refit_matched_kernel_gates.py`；模型和结果 `adaptive_search_results/matched_kernel_gate_{model,evaluation}.json`。

## 实验控制

固定新核，只用其已审核120窗训练L/Q重新拟合原标量、运动、丰富、残差门控。特征、120窗顺序、80前段:40较早后段权重、归一化、8维投影、优化器、正则和回退规则全部不变。先用旧L/Q重拟合并逐位回放旧门控预测，证明流程一致，再替换新核L/Q。九次优化全部收敛且全部选中非回退模型；序列化同批预测逐位回放。

所有拟合和模型冻结之后，才读取晚段40窗/622017–622018/P32缓存系数。窗口video/start、旧门控评分逐位核对。没有重新采样或积分，没有读取hold/DEV/TEST，没有超参数搜索。沿用既有全alpha双网格精度审核及保护失败记录；不是新独立评价。时间历史与目标隔离断言保留。

## 晚段结果（核固定为新核）

| 门控 | 重训前误差 | 匹配重训后 | 重训变化 |
|---|---:|---:|---:|
| 标量 | .222775773 | .224960722 | +.002184948 |
| 运动 | .223329758 | .226063143 | +.002733385 |
| 丰富 | .225921099 | .226574554 | +.000653455 |
| 残差 | .223703703 | .225738903 | +.002035200 |

原点.225261157。四种门控在两个seed上都比重训前差；只有匹配标量仍略优原点（-.000300436），不能抵消其重训退化。匹配运动/丰富/残差分别比原点+.000801986/+.001313396/+.000477746。没有推广，也没有继续读取hold确认这些内部已退化的候选。

## 训练目标中的取舍

下表是同一新核下“匹配重训后−重训前”的非正则误差变化：

| 门控 | 训练总体120窗 | 前段80窗 | 较早后段40窗 |
|---|---:|---:|---:|
| 标量 | -.000180721 | -.000479140 | +.000416118 |
| 运动 | -.000162426 | -.000388461 | +.000289643 |
| 丰富 | -.000195502 | -.000051815 | -.000482876 |
| 残差 | -.000113721 | -.000297946 | +.000254729 |

训练平均均改善；标量、运动以及残差三种门控在训练内就牺牲后段以换前段收益（80:40权重）。丰富门控两个训练区域均改善却仍晚段退化，故不能把全部问题归因于区域权重。旧新核不匹配不是当前失败的充分解释，训练分布/目标取舍与时间泛化仍需处理。

下一候选（未实施）：只在训练侧明确分组风险目标，检查前段与较早后段是否可以同时受益，保留原等窗平均目标作对照，再使用既定隔离晚段评价。不能通过扫描区域权重追逐已见hold，不能对视频18或某时距设特例；区域重加权也未必足以解决丰富门控的泛化失败。

验证：184项测试通过，包括固定拟合的有限预测、概率边界、序列化回放和残差幅度约束；所有输入hash不变。保存全部负结果。
