# TRAIN条件核诊断：状态差异可重复，简单特征关联弱

2026-09-18。脚本 `audit_kernel_training_context.py`，报告 `adaptive_search_results/kernel_training_context.json`。

只读10个拟合TRAIN视频前缀，原80窗口481017。全池18813个可用起点（大量重叠，不是18813独立样本）。原482017/P32全部基准评分逐位回放；新532017/P32原窗口固定U核，全80窗口2049/4097积分门槛通过，maxscore1.78026e-5、grad1.33385e-7。四CPU进程分块评价。162tests、guard0，无hold/DEV/TEST/重拟合/默认推广。

## 覆盖

旧80窗口历史31步增量RMS四分位边界 .112480/.175223/.215667 rad/sample；每箱20窗。用这些纯TRAIN边界衡量全候选池，按视频等权箱比例 .192763/.327818/.223524/.255894。每个视频都存在低箱状态，占13.7%–25.0%；各视频最小RMS .0186–.0508。低运动不是TRAIN完全缺失状态，80窗低箱25%也不低于池等视频比例19.3%；这不证明对所有更细低幅状态都有足够覆盖。

## 新随机流下固定核收益

旧/新综合改变量约−.00521510/−.00307351（均为这些拟合状态）。新0.5/1/3秒变化−.00428890/−.00588706/+.00095543；3秒符号变差，不能只报总体训练改善。

新旧逐窗综合改变量相关 .872448，提示窗口间收益差异有可重复成分。但初始可用特征与新收益线性相关弱：

- log历史运动：−.08048；逐视频分别去均值后：−.03567。
- 初始下一q分布熵：+.09074。
- 原预测sin/cos嵌入方差总和与对应时距收益：+.08340/+.03057/+.10621。

这些不是预测准确率、因果效应或泛化验证；近零线性相关不能否定非线性预测能力。

低→高运动四箱新综合变化+.00003381/+.00027990/−.00953146/−.00307630，非简单单调关系。第二箱旧−.00602152变为新+.00027990，单个新RNG和每箱20窗仍有噪声，不据此选择箱/阈值。

## 限制和下一步

当前核用全部10个视频拟合，故这些状态诊断不是诚实的按视频交叉验证；新动作RNG也不等于新状态/新视频。没有使用留出结果估计阈值，更没有据此编写“低速度禁核”规则。

下一步做真正的逐视频留一：每折从另外9视频重新拟合常数核和低容量状态条件宽度，归一化/阈值也仅用该折训练视频；用排除视频的独立轨迹随机流评价，与零核和同折常数核对照。不能沿用当前全10视频核作为无泄漏的折内基准。先以少量因果特征、固定容量/正则方案验证，不因这些描述性相关扫描大量特征或在hold上调参。尚未实施。
