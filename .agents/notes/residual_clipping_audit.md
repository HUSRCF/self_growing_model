# 无界训练/限幅部署审计

2026-09-17。`audit_residual_clipping.py`；报告 `adaptive_search_results/residual_clipping_audit.json`。

固定observed/absolute/increment三个已存岭模型，重建上一轮TRAIN前半段真实历史与冻结生成历史，10拟合/3复用留出分开。没有重拟合或幅度选择；无界输出只在固定历史上评分，不部署。模型文件SHA未变，实际限幅输出与原correction逐项一致。报告8候选概率加权触发率、嵌入MSE、原wrapped残差二次目标和零幅度处的一阶改善量。正一阶gain表示局部方向有益；不代表当前完整幅度有效。

## 正常真实历史，留出视频

| 校正器 | 至少一维限幅的候选概率 | 无界MSE | 限幅MSE |
|---|---:|---:|---:|
| observed | 0% | 2.13914e-6 | 2.13914e-6 |
| absolute | 93.901% | 2.94426e-4 | 1.99430e-5 |
| increment | 6.747% | 7.74594e-6 | 5.87154e-6 |

零校正MSE2.20715e-6。absolute取消限幅约133.4倍零误差，限幅后仍约9.04倍；increment分别约3.51/2.66倍。限幅明显保护正常状态，取消限幅不构成修复。

## 生成历史，留出视频

absolute触发94.304%，自身绝对位置目标MSE zero .01062898、raw .01044824、bounded .01054489：限幅牺牲一部分局部追回效果，但避免真实历史巨大的伤害。

increment触发7.416%，自身增量目标MSE zero .000107096、raw .000110686、bounded .000109157。即使取消限幅，它在留出视频也没有局部优势。拟合集同目标raw .000550124优于bounded .000558846和zero .000568129，说明原训练目标确实有部署错配，但错配不是整个泛化失败的解释。

## 方向与幅度应分开

正常真实历史留出：absolute raw/bounded一阶gain=1.30269e-7/3.29039e-8；increment=1.08694e-8/3.30123e-8，均为正，但远不足以抵消完整幅度的二阶损害。正常拟合集也为正。生成历史increment目标，increment模型一阶gain同样正，raw/bounded=2.10978e-6/1.72599e-6，完整幅度仍恶化。

这支持用拟合数据限制正常状态损害/校正幅度的下一步实验，不支持直接拿留出梯度挑幅度。平均正方向不能保证每个视频、每个时刻或闭环有益。本次平均0–63步；不推翻此前observed校正器在offset8特定生成快照的负方向。模型、目标、聚合状态不同，必须分别解读。

下一步可将现有增量校正方向保持固定，用TRAIN拟合真实历史上的显式损害界定义保守幅度（保持零校正候选），再做自由生成留出门槛；不能把局部保护当作新模型自身生成分布上的稳定性保证。另一选择是部署一致的有界训练，但不能仅凭本审计承诺其有效。

65测试通过，包含概率加权限幅率、限幅有益/有害两种构造、无限幅恒等及一阶有限差分。无DEV/TEST，无默认/冻结包更改。
