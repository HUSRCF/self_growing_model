# 当前任务定义

更新时间：2026-09-18 UTC

## 已授权的研究扩展

延后干预实际状态TRAIN诊断：核对已有route sensitivity只读快照模式，continuation增加可选指定步复制快照（h/q/读前hidden/失败/RNG），默认输出不变。原80TRAIN/761017–20/P8，记录0/50读前真实递归状态；有/无记录全轨迹逐位一致、50历史与生成点19..50一致、旧baseline逐位回放。仅报告q直方图、窗口平均运动/p6/q6占比/路由熵/hidden RMS，与固定r6长时距集合收益的同流及另三流标签相关/视频内去均值相关。集合统计不直接当单粒子策略证据，不拟合gate/选阈值/晚段/hold/DEV/TEST，4CPU进程BLAS1。

延后r6冻结三域迁移：原781017/P32首确认流全部评分/guard/短期舍入逐位回放，同策略第51点一次r6/.25，拟合视频隔离晚段40窗及历史适配器hold18/16/13前后各24窗，固定791017–791020/P32两个分量、8CPU进程BLAS1。三域全部评价不看中途择域，前50步轨迹/失败精确同原模型。报告全视频/时距/seed，不调参/DEV/TEST/推广；历史留出和骨干TRAIN非项目盲测。

延后策略固定八流确认：先用原771017–20/P16四流重生成原始+延后r6两个分量，baseline逐位、混合评分1e-14匹配旧九分量。冻结r6/.25和第51步一次干预，新781017–781024/P32同80TRAIN状态，8CPU进程BLAS1，固定跑完八流不追加。前50步轨迹/失败逐位相等；不拟合/调时刻/调强度/晚段/hold/DEV/TEST/推广，仅条件采样确认。

延后干预LOVO：原80TRAIN的新761017–20/P8九分量标签，九视频72窗拟合r/alpha<=.25，不扫描干预时间；全量训练解核对、十折概率序列化逐位一致后冻结。新771017–771020/P16每九分量/300，4CPU进程BLAS1，原模型/固定r6.25/交叉拟合三对照。固定r6来自全量TRAIN，明确非排除视频学习对照。每分量前50步完整轨迹逐位核验，短期评分舍入<=1e-14后恢复结构精确零。无晚段/hold/DEV/TEST/推广。

延后一次干预训练诊断：扫描未找到等价实现后，continuation新增forced_step默认0，历史451017/P4全部8根A/B逐位回放保护默认行为。原80TRAIN根，新761017–761020/P8，各原模型+八种在zero-based50强制目的r的分量，前50步整轨迹/失败逐位一致，GRU记忆不重置，两uniform照常消费。九分量完整U积分排除同粒子索引；全局Bernoulli混合原/强制续跑alpha<=.25，按新训练标签报告八根曲线及训练最优，不搬用根时刻权重。0.5秒系数结构为零，仅在1e-14抵消核验后归零。4CPU进程BLAS1，不CV/晚段/hold/DEV/TEST/推广。

全量两档根策略时间迁移：复制冻结现有fullTRAIN两档策略及全量有/无均值短期约束，保留prior/固定r5.25。训练每视频三时距U重放1e-14、JSON预测逐位一致，冻结后才加载同10视频末1/4的40窗，断言历史目标与拟合区间隔离。新751017–751020/P16每根/300，4CPU进程BLAS1，五对照完整报告；不重新拟合/调阈值/hold/DEV/TEST/推广，历史晚段非盲测。

两档根约束LOVO评价：训练可行性诊断通过后，原十折训练中位数、LP方向、二次可行alpha全部冻结，旧prior/固定r5.25/无约束/均值约束四策略逐位回放，新增两档策略概率序列化逐位一致。新741017–741020四流P16每根、300步、4CPU进程BLAS1同80状态排除视频评价，全部五对照，不按评价修改阈值/根/强度；不晚段/hold/DEV/TEST/推广。

两档因果根分布可行性：只用旧80TRAIN历史运动RMS的拟合中位数分组，十折阈值排除视频。每档根系数非负且和<=.25，保留全局统一方案为子集；总系数可至.5，方向实际alpha上限为.25/最大档质量，不额外收紧。逐视频均值分母为该视频全部窗口，恢复分块二次项并直接U核验。16维LP及总alpha<=.5有限充分界、选中方向二次可行强度；不新轨迹/评价/晚段/hold/DEV/TEST/推广，不按视频ID或未来标签分组。

联合根可行性训练诊断：同缓存恢复完整交叉曲率Q，单根对角/随机联合策略直接U恒等式1e-14核对。s为八根概率，min_s max_video短期一阶斜率LP，报告可行原始/对偶界与间隙；对偶加权Q最小特征值给alpha<=.25充分排除界，不把未排除说成可行。全量及十个排除训练折，LP选中方向另求完整二次逐视频可行alpha。不新轨迹/评价/晚段/hold/DEV/TEST/推广，数值证据非区间算术严格证明，不将局部LP当联合有限步最优。

逐视频短期可行域诊断：仅旧80TRAIN/四流P4缓存LQ，按视频平均。每根alpha>0条件等价L_v+alpha Q_v<=0，精确交集加独立零点，支持负曲率与分离正区间。全10视频及每个排除折均报告单视频/交集区间、最优alpha；保持[0,.25]和等视频综合目标。旧均值约束结果1e-14回放，无新轨迹/排除标签评价/晚段/hold/DEV/TEST/推广，不将单根不可行推广为任意联合根策略不可行。

短期约束根混合LOVO：原80TRAIN根/四流P4的LQ，每次排除一整视频，九视频72窗拟合root和alpha[0,.25]，有/无短期均值约束同算法，不调容差/比例。原全量结果核对，所有折模型与prior/固定r5.25对照序列化冻结后，新731017–731020四流P16每根、300步、4CPU进程BLAS1评价排除视频。报告逐视频三时距与失败，约束只在训练侧，不晚段/hold/DEV/TEST/推广。骨干见TRAIN，仅适配器CV。

训练根方向短期约束诊断：原80TRAIN根、原451017–20/P4逐位重放完整A/B与prior评分，并保存三时距独立A/B。八根各自w=(1-alpha)prior+alpha onehot(r)，alpha限[0,.25]，全部方向报告L/Q。枚举端点、综合目标驻点、短期约束边界，求无约束/训练平均0.5秒不退步的精确一维最优，零始终可选；不假定有限P曲率非负，1e-12仅舍入容差。不晚段/hold/DEV/TEST/推广，不将训练均值约束称每视频或泛化保证。4CPU进程BLAS1。

根critic固定高采样确认：全四策略及两区域不变，旧late与历史hold元数据/概率/缓存评分逐位重放。固定721017–721024八流、每根P32、前后各24窗、8CPU进程BLAS1；不根据中途符号追加seed，不训练/选策略/调25%比例/DEV/TEST/推广。旧四流P16报告保留。仅条件Monte Carlo稳定性，不当新视频泛化，短期退步不得忽略。

冻结完整根critic跨视频双区域确认：四全量策略、训练参数/25%比例不变，旧末段概率/评分/缓存分数逐位回放，历史TRAINhold18/16/13前后各24窗，固定711017–711020/P16每根分量、300步、4CPU进程BLAS1。全对照共享完整根积分，跨分量排除同粒子索引；原模型及报告hash核验。报告每视频/时距/seed，不调参/DEV/TEST/推广。留出历史复用及骨干TRAIN非项目盲测，tail首历史可能跨中点。

完整根critic时间迁移：同原80TRAIN/451017–20/P4标签，全量拟合四个linear/full×action/context策略，原LOVO评分逐位重放，模型序列化预测逐位一致，冻结后才加载同10视频末1/4的40窗。新701017/701018每根P8、300步、2CPU进程BLAS1，全部四对照及prior、完整根分布评分，核验历史+目标与critic拟合区间隔离。不hold/DEV/TEST/调参/推广；历史晚段复用非项目盲测。

完整根混合critic匹配试验：原80TRAIN根窗口，旧451017–20/P4完整A/B平均标签。九候选原prior及八个25%向根混合；只比较完整U变化与一阶变化，同九候选特征（原候选特征的分布加权均值）、128固定随机投影、ridge .01N、按视频留一；各有context/action-only对照，不选超参数。四策略全部序列化冻结后新691017/691018/P8每根分量、300步、2CPU进程BLAS1评价，完整根积分排除跨分量同粒子索引。原prior旧评分逐位回放，原模型hash核验，不hold/DEV/TEST/逐步反馈/推广。骨干见TRAIN，仅适配器级视频CV。

根概率选择空间诊断：只复用原80TRAIN根状态451017–20/P4的完整A/B缓存，固定原prior加八种25%向单根混合，另外三流平均评分逐窗选策略、排除流评价，并报告同流乐观oracle与选择一致性。原prior评分逐位复现。未来标签共享，仅不可部署诊断，不新轨迹/hold/DEV/TEST/调比例/推广；折间重叠不计算独立seed显著性。

小容量条件输出偏移：同120训练侧窗口，按视频留一，108窗训练旧482017/621017/P32，折内log历史RMS标准化clip±3。每时距两角clip(b0+b1*z,±.25)，共4系数/时距，系数±.25、.001全部L2、零初值、L-BFGS200，训练不优或失败回退零。共同偏移原折参数逐位回放，全部冻结后新681017/681018P32同120状态排除视频评分。只原点/共同/条件对照，不晚段/hold/DEV/TEST/调参/推广；骨干见TRAIN，非全系统盲测。

输出偏移训练稳定性审计：原120窗固定，原482017/621017及651017/651018两独立动作流P32，6CPU采样；每流全量及逐一排除10视频拟合，共99次同规格2D偏移拟合，原全量shift逐位回放。全部拟合冻结后做3×3流间同状态评分及排除视频评分，报告偏移范围/边界/失败/视频误差，不把同未来标签跨RNG当泛化。仅adapter排除视频，骨干已见TRAIN；不晚段评价/hold/DEV/TEST/调参/推广。

输出中心平移pilot：原120训练窗/482017与621017/P32基线逐位回放，每时距2个共同角偏移共6参数，固定±.25rad、.001L2、零初值、L-BFGS200，训练不优回退零。共同转动保持嵌入粒子对距离，精确零偏移及解析吸引项梯度先单测。冻结后隔离晚段40窗、新661017–661020/P32四seed，只比原点/平移；不加核、不更新门控、不反馈F、不hold/DEV/TEST/调参/推广。非收敛停评价，边界原样报告。

拟合侧误差几何审计：仅10拟合视频原80prefix+40earlytail，原482017/621017基线逐位回放，再651017/651018各P32，6CPU进程BLAS1。计算嵌入平方误差的中心误差/粒子方差精确分解，另列U-energy吸引项/多样性项、最近粒子距离、圆均值集中度及两个角度sin残差相关；逐视频逐时距报告，低集中度圆均值不稳定明确标记。描述性诊断，不把粒子当独立样本、不把相关性当缺失核协方差证据，不拟合/晚段评价/hold/DEV/TEST/推广。

区域风险预定八seed复核：冻结新核、五风险候选及原点/先前旧运动，历史hold前后各24窗固定641017–641024/P64，16CPU进程BLAS1，全alpha2049/4097原门槛，跨seed alpha逐位一致，完整报告全部策略/视频/时距。跑完八seed不因符号追加，不调参/DEV/TEST/推广；仅固定窗口条件采样复核，不算新视频泛化，旧四seed单独保留。

区域风险冻结筛查：读取既有新核631017–20/P64历史hold前后各24窗缓存，对全部5个风险候选保留原点/先前旧运动对照，原九门控评分逐位回放，全alpha精度重新检查。仅因果历史计算alpha，无重新训练/采样/积分/DEV/TEST/调参/推广；复用历史hold明确不算新独立确认。

训练区域风险固定试验：新核及120窗L/Q固定，只比标量和log运动clip-affine门控。目标为两个训练时间区域“相对原点误差变化”的最大值+.001斜率平方，而非绝对误差max。标量枚举端点/各组正曲率驻点/组间交点得到全局minimax；运动SLSQP epigraph、单[.5,0]初值、原边界、200步、训练精确标量回退，报告约束违反。增加相同SLSQP的原等窗mean控制及保留旧LBFGSmean，避免混淆优化器改变。冻结后仅缓存隔离末段40窗评价，旧mean预测逐位回放；不扫描权重、hold/DEV/TEST/新积分/推广。

新核匹配门控校准：仅用已审核120训练窗的新核L/Q、原61维因果特征，原标量/运动/丰富/残差算法、超参数、归一化、投影不变。先用旧L/Q完整重拟合并逐位回放旧预测，再切换新核L/Q拟合，序列化冻结后才读取隔离晚段40窗/622017–18/P32缓存系数评价。旧评价分数逐位回放，全部候选报告，不新采样/积分、不hold/DEV/TEST/调参/推广；历史内部评价复用非独立确认。

冻结核宽双区域确认：新旧核×九门控全部保留，源hash/训练审核先检查，历史hold18/16/13前段和后段各24窗，新631017–631020/P64共享轨迹。4进程采样、16进程积分BLAS1，全alpha2049/4097原门槛不变，跨核原点/跨seed门控逐位一致。报告全部策略/逐视频/时距/seed和失败，不按结果调参/DEV/TEST/推广。历史hold及骨干TRAIN非盲测；后段首历史可跨中点，区内目标重叠。

时间隔离核宽重估：复用原80prefix/482017及40earlytail/621017 P32训练轨迹，旧baseline逐位回放；固定原运动阈值，只重估两档×三时距×二维核宽。1025 raw smoothed U（与旧anchored相差与参数无关常数），单log10初值、边界1e-4..1e6、200步，训练内精确点回退。6进程拟合，冻结后120训练窗8进程全alpha2049/4097原门槛审核，失败保留不细网格重拟合。门控/F/GRU不变，不读取晚段/hold/DEV/TEST；通过后再做旧核与新核同门控配对评价。

核宽配对评价：训练审核通过后，同旧末段40窗及622017/622018 P32轨迹重放，九种门控alpha与旧保存结果逐位核对，原点baseline逐位核对；只把旧核替换为训练侧新核。报告所有九策略和逐视频/时距/seed，不更新门控或阈值；相同历史内部验证与随机流不是独立确认，不hold/DEV/TEST/推广。

时间隔离门控训练协议变更：仅10拟合视频，原80前缀系数复用，加视频中点至3/4区间4窗/视频，历史和300步目标均在该区间；训练权重120窗均等（前后80:40），seed621017/P32。固定核不重拟合。比较新标量/运动/丰富门控与运动基底+.25tanh(固定8维投影+截距)残差，所有残差系数.001L2、单零初值、边界±2、失败或训练正则损失不优回退运动。冻结后读取末1/4区间4窗/视频，历史目标均在该区间且与训练无交叠；新622017/622018 P32，旧五策略保留，全alpha2049/4097原门槛不变。4积分进程BLAS1。既有核/骨干不变，不hold/DEV/TEST/推广；历史视频已看过，不称项目盲测，不按评价调参数。

拟合侧冻结收益审计：现有全量核/五策略不变，10拟合视频每区域8均匀新起点，排除原80同起点；后段历史整体过中点。新611017/611018各P32，4进程生成完整80窗轨迹，8进程分20窗精确L/Q积分，全alpha2049/4097原门槛保留。无重新拟合/hold/DEV/TEST；前段目标可与拟合重叠，因此只是时间区域迁移描述，不称时间交叉验证。完整报告逐视频/时距/seed和失败。

拟合侧时间覆盖只读审计：仅10拟合TRAIN视频，每视频每区域64个均匀唯一历史起点，排除原80起点，后段历史整体位于中点后。固定原全量门控归一化/投影/权重，比较运动、q、裁剪率、排除同视频的原80参考近邻距离和alpha。无目标提取、rollout、标签、拟合、hold/DEV/TEST；区内重叠明确，不据此宣称因果或泛化胜出。

全TRAIN软门控与双区留出：固定既有全量两档条件核，原10TRAIN80窗481017/482017P32，全alpha精确点LQ2049/4097原精度门槛通过后拟合标量/运动/丰富门控。复用既定61维→固定8维tanh/正则/边界/回退，所有候选序列化预测逐位回放，写出冻结模型后才加载hold18/16/13前段和后段各24窗。新601017–601020四seed/P64，训练系数4CPU进程、评价8进程BLAS1；原点/全核/标量/运动/丰富五组一致重评分，全alpha门槛不变，不hold调参/DEV/TEST/推广。

冻结五策略新起点验证：原点/完整条件核/固定权重/运动门控/丰富特征门控全部冻结，源hash和旧特征alpha逐位复现。同10TRAIN前缀每视频8窗，seed591017排除旧481017完全相同起点；量化旧目标/历史区间重叠，不当新独立视频。新592017–592020四动作seed/P32，准备4CPU进程、积分8进程BLAS1；新全窗全alpha2049/4097原精度门槛。报告全部策略/seed条件SE/视频/时距，不训练/调投影/正则/hold/DEV/TEST/推广。

丰富因果特征软门控固定试验：保留各折条件核/旧TRAIN精确LQ/原点/全核/标量/运动门控，新增原历史连续特征+源q onehot+当前readhidden+log运动。每折仅9视频归一化clip±3，固定seed1901投影8维tanh+截距，每时距9参数，同.001斜率平方惩罚/边界/初值/200步及标量回退。容量比运动门控大，不称纯表征同容量比较。全部模型先冻结，旧运动alpha及标量逐位复现，再新581017/581018同80窗/P32排除视频评价，8CPU进程BLAS1，全alpha双网格原门槛。无特征/随机投影seed/正则扫描、hold/DEV/TEST/推广。

软门控相对标量未获益后事后oracle诊断：仅读取已保存571017/18排除视频系数，逐窗/时距用未来真值的L/Q选alpha，再分别同seed及另一seed评分。明确不可部署、同窗口未来标签泄漏使其不是新泛化证据；跨RNG仍非严格上界。仅判断潜在选择空间/权重重复性，不更新模型或读取hold。

软核门控LOVO：冻结旧各折9视频条件核，仅该折72TRAIN窗482017/P32计算精确点L/Q，2049/4097全alpha原score1e-4/grad1e-5先验收。每时距比较alpha0/1、精确标量二次最优alpha、clip(b0+b1*z,0,1)；z为仅折内拟合log(历史RMS+1e-12)标准化并clip±3。固定b0[-2,3]/b1[-2,2]、单起点[.5,0]、.001*b1²、L-BFGS200；不收敛或正则TRAIN目标不优则回退精确标量。训练10CPU进程，所有门控冻结后新571017/571018同80窗/P32排除视频评价8进程；四对照一致采用精确点新公式。无hold/DEV/TEST/推广，backboneTRAIN仅适配器CV。

软混合原锚定alpha梯度门槛失败后追加精确点项对照：不含噪声的A0/B0用原距离精确计算，仅含噪声项保留谱积分；L修正+(A0spect−A0)−(B0spect−B0)，Q修正+.5(B0spect−B0)。alpha0仍精确，alpha1改为raw平滑积分（非旧锚定有限网格值），端点差应为旧baseline_error。旧80窗L/Q/评分逐位复现且旧FAIL保留，原全alpha门槛不变，不训练/新hold。

零核＋连续核软混合审计：独立逐粒子Bernoulli(alpha)使用噪声，不是整集合二选一。推导J=J0+alphaL+alpha²Q，alpha0精确点退化、alpha1复现旧核、混合粒子交叉项/有限差分/直接离散噪声积分先单测。固定旧全TRAIN U核及80窗481017/482017/P32，2049/4097全alpha[0,1]多项式差最大值与alpha梯度差最大值按score1e-4/grad1e-5检查。仅预定alpha0/.25/.5/1描述，不调alpha、不门控训练/hold/DEV/TEST；历史留出失败保持。

两档核全量拟合与双区域留出：原10TRAIN80窗481017/P32轨迹482017，完整零分数逐位回放，历史运动中位数分两档各40窗，同CV fit_one配置重新拟合6组2角核，训练不优于零可退回零；常数核直接冻结原全TRAIN U模型。全部拟合收敛/写出模型后再加载hold18/16/13前段和后段各24窗，新561017–561020四seed/P64，8CPU进程BLAS1。两区全窗2049/4097原score1e-4/grad1e-5门槛，全部候选/逐视频/时距/seed报告。不按hold选择阈值或宽度，不DEV/TEST/推广。

冻结LOVO核确认：10折常数/两档条件/零全部保留，源模型/原评分/F/GRU hash不变；旧541017/P32全80零评分、首折首窗双模型双网格cost/grad逐位重放。新551017–551020四seed/P64同80窗，轨迹量相对旧2seed/P32为4倍，准备4CPU进程、评价8进程BLAS1。所有窗2049/4097原score1e-4/grad1e-5门槛；配对seed条件SE/逐视频/时距全报。无重训/调阈值/调边界/hold/DEV/TEST/推广。

核宽适配器LOVO：10拟合视频旧80窗/482017/P32，每折剔除整视频，用9视频72窗重新拟合常数核和按当折历史运动中位数分两档的条件核（6 vs12核参数）。各时距1025U/单log10/1e-4..1e6/200步，训练均值不优于零则该核退回零，不按排除视频选模型。全部90拟合冻结且收敛后新541017/541018两动作流同80窗/P32，排除视频评分双网格2049/4097原门槛。训练4进程、评价8进程BLAS1。所有折阈值/核不读排除视频真值，backbone仍原TRAIN故只称适配器级CV。无hold/DEV/TEST/推广。

TRAIN核状态诊断：仅原10拟合视频prefix全候选池及旧80窗481017，运动RMS四分位边界由80窗确定，报告各视频池覆盖；旧482017/P32全部零评分逐位重放，新532017/P32同窗口固定U核双网格2049/4097原门槛。描述运动/视频内去均值运动/原预测离散度/初始路由熵与收益的相关及分箱，不拟合gate；核本身已用全部10视频拟合，不冒充LOVO或新视频验证。无hold/DEV/TEST/重拟合/推广。

尾段负结果后追加只读状态描述：原前后各24窗历史31步角增量RMS、逐视频中位数、与各窗8seed平均U改变量的log运动相关。不调阈值/拟合条件核，不把相关性当因果或192独立窗口；事后诊断明确标记。

冻结输出核时间区域迁移：U/NLL32/zero源hash不变，TRAINhold18/16/13后半段各8窗/P64，复用前段确认的521017–521024八seed作区域对照，8CPU进程BLAS1。后段目标与前段目标无交叠但区内窗口可能重叠、首后段历史可跨边界；不是新独立视频或因果归因。所有后段窗口双网格2049/4097原精度门槛，报告逐视频/时距/seed及相对前段变化，不重拟合/选择/DEV/TEST/推广。

冻结U核高采样确认：完整保留U/NLL32/zero，原24TRAINhold窗口，新521017–521024八seed/P64（旧4seed/P32），8CPU进程BLAS1，不等预算且轨迹采样4倍。旧511017全部24零核分数逐位复现，首窗口两核/两网格/三时距cost/gradient逐位重放。新全部窗口2049/4097原score1e-4/grad1e-5复核；源hash不变，报告逐视频/时距/配对seedSE。无重拟合/选择/DEV/TEST/推广。

冻结能量核留出检验：U拟合核/同32中心NLL核/零核全部冻结并校验源hash；原TRAINhold18/16/13前缀各8窗/P32/新511017–511020四动作seed，四独立CPU进程各BLAS1。无附加核噪声抽样，逐窗确定性积分2049/4097、原score1e-4/gradient1e-5全部检查；报告所有策略、seed配对差/条件SE/逐视频/时距/失败，不按结果调核或挑时距。历史复用hold非全项目盲测，不DEV/TEST/推广。

冻结U核完整细化：不只抽最差窗，原80TRAIN/482017/P32全量2049与4097比较，分离raw/零核谱误差/锚定评分/梯度；全频率按128行分块，无频率截断。2049对旧保存cost/grad以atol1e-13、rtol0重放。原score1e-4/grad1e-5及1025→2049失败保留，不重新训练/hold/DEV/TEST。分块对原实现、不同块大小及零核先单测。

直接能量目标核宽pilot：固定旧80 TRAIN窗口481017/P32轨迹482017，原NLL逐位回放，冻结中心/动力学；按窗口平均1025谱统计量优化每时距2参数，共6个，单起点log10，log边界1e-4..1e6、L-BFGS-B maxiter200/gtol1e-7/ftol1e-12。零核对照始终报告，不按训练收益自动推广。全部拟合冻结后80窗口逐一1025/2049复核原score1e-4/grad1e-5，不调参/重拟合/hold/DEV/TEST。聚合线性恒等式先单测；非收敛/边界/分辨率失败均原样报告。

谱积分后续分辨率检查：513→1025锚定评分约1.495e-4仍失败、raw和梯度通过后，追加2049并复算前三级；原门槛/失败不变，非预先一次性四网格试验，不用结果拟合模型。

谱积分细网格复核：固定上一轮10 TRAIN案例/旧核参数/原482017完整80窗P32采样，逐窗逐时距分块计算257/513/1025，257对旧批量结果atol1e-13/rtol0重放；保留原失败记录，257→513及513→1025均报告原score1e-4/gradient1e-5门槛，同时分离raw评分与零核锚定误差。网格差不是严格绝对误差界。不训练/hold/DEV/TEST/推广。

输出核谱积分/梯度审计：保持旧32/new128核，原TRAIN482017/P32轨迹，10视频各取旧8窗中的首窗，不训练/hold/DEV/TEST。二维周期嵌入距离Fourier网格33/65/129/257，粒子对特征(P|phi|²−1)/(P−1)排除自对；独立核噪声乘子平方。零核锚定原精确U，承认有限网格近似；预定129vs257逐窗最大score差1e-4、gradient差1e-5，失败如实记录不放宽。先离散噪声直接积分、Bessel矩数值积分、有限差分/周期性测试；固定new128核另512噪声抽样比较，不把条件噪声SE当视频泛化。

输出核训练粒子覆盖对照：同80拟合窗口481017，保留旧32中心482017，加482018–482020三批独立32轨迹，嵌套128；优化器/6参数/初始化/边界不变，旧fits须完整精确复现。冻结新旧kappa后，旧483017留出评分精确回放，新491017–491020/hold24/P32与noise seed+1e6比较zero/old32/new128。只增加TRAIN密度估计样本，不按hold调宽度；4倍拟合采样成本明确，vonMises不同参数未必共享同一扰动流，不DEV/TEST。

周期输出核pilot：冻结原动力学/事件/粒子权重，仅50/100/300步输出角度中心叠加独立乘积von Mises核。TRAIN10前缀每视频8窗seed481017，原32粒子seed482017；每时距拟合2个kappa，共6参数，有限粒子混合NLL，logk初始log10、边界1e-4..1e6、L-BFGS-B最多200，无多起点/尺度扫描；失败轨迹或不收敛停止而非剔除。冻结后hold24/P32、新483017–483020与独立噪声seed+1000000，原点/零核/非零核对照U-energy；不以点分布无连续NLL冒充密度胜出。扰动不入历史，只有输出边际，不声称联合物理轨迹，不DEV/TEST。

冻结根混合高精度确认：从root_mixture_holdout读取全部模型/校准量，不拟合；原24窗口分数、根概率、旧40保存评分须逐位复现，旧461017/P4的A/B重新生成须精确。全部10策略在同24TRAINhold窗口、新471017–471024八RNG/P8每分量/300复核。只增加评价计算量，不改分布/比例/目标，不宣称等预算或新视频，不DEV/TEST。

固定根混合TRAINhold门槛：原10拟合视频80窗口、431017/18标签，按原128随机特征/ridge .01*N拟合三个完整critic，JSON序列化预测须精确；软强度只在拟合状态校准meanKL .01。模型冻结后读原3TRAINhold视频18/16/13前缀各8窗口，新461017–461020/P4每根分量/300，以完整iid根积分评价原prior和三种hard/soft/固定25%mix全部10策略。留出视频历史复用，不称全项目盲测；不选择/调参/DEV/TEST/逐步反馈。

完整iid根分布审计：冻结三个匹配目标critic，保留prior、各hard/soft及固定25%hard+75%prior（共10策略），新451017–451020/P4每分量/300、旧80拟合窗口。枚举8根条件分量，J(w)=wA−.5wBw；跨分量B也排除共用随机流i=j，只用独立粒子索引。先toy穷举独立根分配验证、one-hot还原原U、线性+二次变化恒等式。禁止把固定一个新粒子和三个旧粒子当iid分布，不扫混合比例/hold/DEV/TEST/逐步接管。

匹配目标critic对照：仅TRAIN10视频80根窗口，旧431017/18标签分别拟合U/MSE/attraction，保持原候选因果特征、128随机特征、ridge .01*N、LOVO和每折meanKL .01软调整。旧431019/20仅复用诊断，所有三模型及硬/软选择先冻结，再新441017–441020/P4/300做单粒子U动作标签确认；不按旧诊断筛选模型或调温度。只一粒子根干预，不推广到全粒子/每步反馈，不hold/DEV/TEST。

U-energy单粒子根动作标签审计：旧80拟合窗口/P4/300、新431017–431020，前2标签选择诊断oracle、后2独立评价。每r仅替换一个粒子的完整条件续跑，其余3保留原样本；平均四次独立干预，不同时改4粒子。每时距验证ΔU=(Δtruthdistance−Δmeanpairdistance+2Δfailed)/P与原energy_costs一致，匹配原根r的轨迹须逐位复现。报告U/MSE/attraction/spread排序重复性与不可部署oracle；不训练critic、不hold/DEV/TEST。

小KL事件内评分倾斜：保留四个8/32覆盖×context/action-only交叉拟合critic，Tnew(r|e)∝Told(r|e)exp(-lambda*score(r))，p(e)不变。每折仅训练状态校准mean joint KL(new||old)=.01，固定lambda上限1e6，非逐状态保证；复现旧scores/零prior。全部概率在新421017–421020/P4/300续跑标签前冻结，原80窗口根边际精确求和评价单轨迹MSE，保留零与硬argmin。无调温度/选择/逐步反馈/hold/DEV/TEST。

值函数上下文与覆盖对照：保持旧critic容量/正则/leave-one-video-out，加入仅按目的r平均代价的无上下文基线。TRAIN10视频每个原8窗口嵌套增加24起点(seed411017，排除旧完全相同起点，允许时间重叠)，新标签412017/18/P4/300，旧标签不变。比较8/32窗口×上下文/无上下文四组，在旧80窗口与旧403017/18评价标签上审计，明确是复用诊断非独立确认。先复现旧prior和scores，不调参/hold/DEV/TEST；4倍采样成本非等预算比较。

候选值函数交叉验证：TRAIN拟合10视频前缀各8窗口(seed401017)，全部8目的r根干预后正常随机续跑P4/300，动作402017/18拟合标签、403017/18独立评价标签。目标50/100/300的单轨迹sincos4D MSE+2失败，不冒充U-energy。按视频leave-one-out，全部归一化排除该视频，固定128随机特征+中心化ridge .01*N，无调参；对照原prior加权、prior众数、拟合标签oracle和评价标签oracle（后两者不可部署）。先原unchecked正常轨迹精确回放。只观察根状态排序，不接管每步事件，不hold/DEV/TEST。

等采样预算首步实效检查：冻结旧控制变量审计的梯度，构造raw8、raw16、8fit+8evaluation控制变量、raw16反方向及零。沿用Adam首步lr.1/eps1e-8/clip1/box1；新动作seed391017–391020，P4/300，原10拟合窗190101及新10拟合窗392017。全部候选先冻结，不挑方向/步长；旧evaluation梯度本轮转为TRAIN拟合数据，新前向RNG独立。不读hold/DEV/TEST；同采样预算不等于总CPU，窗口仍可能时间重叠。

评分控制变量验证：保留原LOO混合梯度G，控制S=总路径logprob对参数梯度，固定标量b时用G-bS。toy穷举4096联合路径验证E[S]=0、期望保持、总体最优b降协方差迹，另展示同样本拟合b的偏差与独立拟合的期望保持。通过后固定theta0/10拟合窗口/P4/300/50detach，以旧371017–371024八seed拟合每窗口标量b，新381017–381024八seed独立评估；旧G须逐项复现。不训练/hold/DEV/TEST，不把保持截断估计器期望称为修复截断偏差。

截断source-q梯度方向复现审计：固定theta0、拟合窗口seed190101、10TRAINprefix/P4/300、50步detach，8新动作seed371017–371024（窗口j加j*100000），记录16维梯度、路径项、每q源状态访问量和逐窗口梯度。共享梯度用已验证sum_q链式关系还原；报告协方差迹、均值/RMS-SE、成对/留一/前四对后四方向余弦，不把访问步数当独立样本，不裁剪/训练/hold/DEV/TEST。

source-q小容量校正pilot：原F后增加当前寄存器q索引的8×2偏移，逐元素复用旧bound，零初始化/盒限制；非目的r，也不诊断重标q。先NumPy零/共享表300步精确退化，Torch共享表前向及对所有q梯度求和=共享梯度验证。通过后同共享截断pilot的3seed×4更新、TRAIN10prefix每视频1窗/P4/300、Adam lr.1/clip1/50步detach、hold0/1/2/4与301017/301018选择，保留零和全部优化seed。然后冻结source-q及共享候选，TRAINhold前缀与尾段各24/P8、4新361017–361020采样seed复核，不DEV/TEST/超参扫描。

TRAIN同视频时间区域诊断：冻结全部候选，3个原留出视频尾段各8窗口/P8/300，与既有前缀341017–341024八seed结果配对；先核对40个旧轨迹产物元数据及逐窗口U。按10拟合视频前缀池预定motion四分位描述两区域因果历史速度RMS与q分布，不拟合或按DEV设阈值。不重选/改参数/DEV/TEST，时间区域关联不能当受控因果效应。

冻结混合writer开发集检查：TRAIN确认后保留全部完整/截断三优化seed的原选择，DEV32尾段窗口/P8/300，固定1729/2718/3141。原零校正的prediction/failed/truth/video/start须逐位复现历史NPZ，选择源哈希不变；报告全部候选、三时距RMSE、综合U、逐视频U和失败数，不按DEV重选/训练/调参/推广，TEST不动；DEV历史已多次使用，不称盲测。

截断候选固定8seed确认：全量冻结旧完整/新截断3优化seed选择，先精确复现331017所有模型评分，再TRAINhold24/P8/300、新341017–341024八动作seed。保存逐窗口轨迹和真正U-energy逐视频综合，报告每候选相对零与同优化seed完整版本配对差。源选择JSON哈希不变，不重选/调参/DEV/TEST，不把随机seed当独立视频。

固定50步截断反传对照：只detach历史/hidden，不改前向/采样/完整300步U目标或旧联合event评分项；toy展示非零梯度偏差，3已定位路径前向逐位核对。通过后复用完整混合pilot的3seed×4Adam更新、窗口与动作seed、P4、边界、lr、clip和hold检查点；不与边际化/因果score混入。选中模型冻结后与原完整模型一起在新331017–331020四动作seed同TRAINhold复核，零保留、不重新选择，不DEV/TEST。

前向切线交叉验证：复用长路径审计3个固定e/r案例，torch forward_ad双数逐步传播，关闭反向计算图；theta两基方向，比较路径/固定优势surrogate梯度与已有反向梯度，零前向逐位复现。记录位置/hidden/logprob切线范数；6个50步注入块分别传播至终点，检查块梯度之和等于完整梯度，前向轨迹不变。不训练/DEV/TEST，不把同公式两种AD一致冒充独立物理验证。

长路径梯度条件审计：固定上轮321020/video15/start121和321023/video4/start741的e/r路径，加入321017/video12首窗口对照；theta0，P4/300，零回放须精确，复现上轮边际混合梯度。记录10/50/100/150/200/250/300时距路径损失与mean logprob导数；归一化theta差分epsilon1e-3/1e-5/1e-7/1e-9。有限差分必须固定基准优势，区分可微surrogate与重新采样的期望目标；逐一记录守卫跨越，不训练/剔除窗口/DEV/TEST。

event边际化梯度：当前F/readhidden无直接e依赖，保持原两uniform采样轨迹，score由logp(e,r)改为logsum_e p(e)p(r|e)。先toy逐r路径验证条件评分导数恒等式及P3 U-energy期望梯度；真实固定theta0/10窗口/P4/300/8seed复用上轮，旧完整梯度须精确复现，记录每窗口梯度与协方差迹。不训练/裁剪/DEV/TEST，不把边际化当解决硬失败边界的证明。

因果时距score诊断：先在2步穷举toy上证明50/100/300对应的多时距类比目标使用完整logprob与各自前缀logprob期望梯度相同；再固定theta0、10拟合视频窗口(seed190101)、P4/300，8配对动作seed321017–321024，比较路径项/完整混合/因果混合的原始梯度协方差。每窗口动作seed加j*100000，固定窗口不刷新；不裁剪/训练/留出选择/DEV/TEST，不声称小样本方差结论普适。

混合梯度固定复核：完成上述3seed后冻结各自TRAIN选定检查点，全部保留，以相同TRAINhold24/P8及4新动作seed311017–311020对照零校正；不重新挑检查点或优化seed，不调参/DEV/TEST，报告配对差及条件seed SE，不当作新视频泛化证明。

真实混合梯度初试：原F每步二维常数角校正，既有TRAIN拟合cap的1%作为固定边界，theta初始0且投影[-1,1]。3优化seed1901/2718/3141各4次Adam更新(lr=.1，梯度norm裁1)，每次刷新10TRAIN拟合视频各1个300步窗口、P4，逐窗口反向累加省内存，完整轨迹U-energy(50/100/300)+留一粒子score项，不截断历史导数。发现任一失败或非有限梯度则整次更新跳过并报告，不静默替换。原NumPy在TRAINhold24/P8/2固定新seed301017/301018评估0/1/2/4检查点，零始终可选；保留全部3优化seed，不DEV/TEST。本轮非与黑盒搜索等预算比较，不宣称硬边界全局无偏。

可微桥接正常采样长路径审计：TRAINhold24/P8/300，固定新seed291017/291018，NumPy相同uniform抽样，比较正常轨迹、事件/q、失败掩码和hidden。严格复现原unchecked基线失败后位置固定但history滑动、q/hidden继续更新的行为，不误用积分writer全状态冻结语义。原atol1e-7门槛失败须如实记录，追踪累计漂移及评分差，不事后放宽门槛；不训练/DEV/TEST。

真实F/GRU可微桥接审计：CPU float64冻结权重，移植summary特征、polynomial/MLP F、分区与GRU读/初始化。TRAIN10prefix各4历史共40，比较特征、全部64(q,r)候选、初始q/hidden及读概率；10步固定动作路径前向核对，连续两参数每步角偏置的路径损失与路径logprob梯度对有限差分。仅审计不训练，不称已处理硬失败守卫/所有非光滑边界；不DEV/TEST。

完整混合梯度小例验证：2步/2事件/2q、含连续历史和递归hidden的冻结路由头，2维连续writer参数，穷举16路径及P3的4096联合粒子组合。U-energy精确期望梯度对照“路径导数+detach优势×路径logprob”的期望梯度、无基线及留一粒子基线、有限差分；另展示仅路径/仅score遗漏。参数取3个预定点，不训练真实模型、不读DEV/TEST。基线仅减score项且detach，整个路径对后续路由概率的依赖不得detach。

原F首点连续扰动：仅10TRAINprefix各256个一步起点，冻结p(r)加权全8个F残差，视频等权中心协方差；不移动F均值、不用积分速度协方差。独立RNG只在首个生成点加一次二维高斯（固定尺度1），后续原F/GRU/失败语义完全不变。零扰动须精确复现旧211017原F轨迹，首步事件不变。原F/首点分布在TRAINhold24/P8/300、新seed221017–221020、noise seed+1000000对照；无调参/DEV/TEST。

256覆盖初始化器固定复核：原F/原初始化/h1/h10全部冻结，先精确重放201017四组预测，再用8新动作seed211017–211024在相同TRAINhold24/P8/300评估。报告每模型、逐视频、配对均值及条件随机SE，保留h1对照，不挑最好seed。不拟合/调参/DEV/TEST；新seed不是新独立视频。

初始化训练覆盖对照：固定前轮方法，将每拟合视频32起点改为256（总320→2560），h1/h10都扩充，旧脚本默认32行为与旧产物保留。归一化/容量/正则每样本强度/优化轮次/边界不变；线性等距网格非严格嵌套，窗口重叠，不是新增独立视频。沿用201017–201020筛查种子以配对比较，原F/原初始化预测须精确复现旧产物，报告两种覆盖的训练/hold参考路径和正常生成。无新DEV/TEST、无调参或挑窗口。

多步初始化替代目标pilot：10TRAINprefix各32固定窗口、可用10步，共320；一步/10步对照同窗口同137因果特征及原scale，学习相对原初速的残差，ridge N*.001，推理每维限±.02。训练只用观测q边序列作固定路径，6次逐窗口有限差分Gauss-Newton（epsilon1e-5、阻尼1e-6），每次保留当前/半步/全步最佳，反求初速不超过原值±.02。10步优化所有10步误差均值，不称无偏闭环训练；计算量不匹配一步。保留原F/原初始化/一步/10步，hold24/P8/300、4新seed201017–201020，正常GRU事件采样评估。无超参扫描/DEV/TEST。

writer适配初始化器：仅10TRAINprefix，与原初始化器相同逐帧池/137因果特征/归一化/ridge N*.001。固定GRU边际p(r)，反求平均位置更新 delta=v+.5*E[a(y,v)|q] 的有效v，8次固定点迭代，每维限制在原学习v±.02rad/sample。目标仅匹配下一步均值，不声称完整期望MSE最优；检查残差/限幅。重新拟合相同容量初始化器，原加速度/q边不变，无噪声。真实历史hold单步按原stride8全边概率加权；原F/代理初始化中点/适配初始化中点，TRAINhold24/P8/300、4新seed191017–191020。未来帧只作TRAIN标签，推理不读未来，不DEV/TEST/调参。

固定writer RK4试验：原加速度系数、局部(q,r)项、学习初速、GRU均冻结。RK4四阶段同一已选边，单位step=1sample，无内部事件重采样。先13TRAINprefix stride8真实历史概率加权全8边单步比较，复现旧中点单步；再原F/点初速中点/点初速RK4，TRAINhold24/P8/300、新seed181017–181020，交替积分器运行顺序并记录完整rollout含评分CPU、不含文件压缩。原中点131017整轨迹精确复现，无噪声混入、无调参/DEV/TEST。

积分writer单步偏差诊断：13TRAIN前半段先截断再9点4阶中心平滑速度/加速度标签，每8帧固定取样，真实32帧历史初始化GRU和q，对全部8个下一q按冻结边际概率精确加权。比较原F、学习初速积分、代理初速积分、学习初速+代理加速度、代理速度+代理加速度Taylor；后3类含未来代理，仅离线诊断。另比较真实下一q边（也仅诊断）。分解学习积分原始角残差为速度误差+.5加速度误差+Taylor余项及交叉项，视频等权，无拟合/闭环/DEV/TEST。

局部路由读取敏感性：重放既有4seed171017–171020全局/条件轨迹，取读前步0/1/5/10/25/50/100/200/299的h/q/hidden，8种交叉替换纯读取，不提交或改变轨迹。比较联合p(e,r)、边际p(r)的TV，解析相同两枚uniform的事件/目的q不一致概率，记录实际分歧与历史差距；另报历史wrappedRMS<.001的子集。反事实h/q/hidden组合可能脱离模型常态，不把TV差唯一分账。无拟合、调参、DEV/TEST。

固定路由路径干预：4既有seed171017–171020、TRAINhold24/P8/300，记录全局和条件模型各自正常生成的event/source/destination；以各自与对方q边路径回放两种初始化，共G/G、C/C、C/G、G/C四格。自路径预测/失败须精确复现，正常轨迹也复现旧NPZ；路径来自模型、无未来真值选择。固定路径不读取干预历史的GRU来改变动作，数值失败仍冻结h/q/v；非部署算法，双向锚定避免把单一路径差解释成唯一因果贡献。无拟合/调参/DEV/TEST。

条件协方差交叉RNG复核：冻结上轮条件模型，用既有动作161017–161020×初始化1161017–1161020全16组合补跑条件分布，复用全局分布已存NPZ；逐项核对元数据及全局原评分，并重放原条件171017轨迹。报告综合、.5/1/3秒以及逐视频配对差，行列均值与描述性分解，不把16格当独立样本。无重训、调参、DEV/TEST，不挑种子或窗口。

固定轨迹差异分解：仅读取conditional/global四动作seed171017–171020已存NPZ，校验truth/video/start一致和既有综合评分。逐时刻U-energy拆分真值距离、集合离散度扣项、失败惩罚；按视频、拟合阈值起点速度组及窗口汇总配对差和总体加权贡献，保留所有窗口。排名仅事后诊断，不作为推理oracle/窗口筛选。不训练、不新rollout、不DEV/TEST。

速度四组协方差试验：10TRAINprefix逐视频外折初始化残差，按拟合池末帧增量范数四分位分组，等视频总体权重条件化估计中心协方差，固定90%组协方差+10%原全局协方差收缩。均值/动力学不变，起点采样一次；无尺度扫描。先报告3TRAINhold速度代理局部校准/NLL，再原F、点积分、全局分布、分组分布在hold24/P8/300、4新动作seed171017–171020与独立初速度seed+1000000对照。全局等协方差退化须复现旧路径。无DEV/TEST，不因局部校准改善宣称轨迹提升。

初始化残差校准诊断：固定现有10prefix拟合初始化器与交叉拟合全局协方差，只读取13TRAIN前半段。按因果历史q和末帧角增量范数分组，速度四分位阈值仅由10拟合视频确定。报告等视频总体权重条件化的残差均值/协方差、相对全局高斯的二维椭圆覆盖及尾部；拟合内残差明确标记乐观。无重拟合、无rollout、无DEV/TEST、不用hold调整协方差或尺度。

固定初始速度分布随机性审计：协方差直接读取已保存初速度分布报告，模型不重拟合；4新动作seed161017–161020 × 4新初始化seed1161017–1161020全交叉，TRAINhold24/P8/300步。每个动作seed的原F/点速度基线仅计算一次，禁止把16格当成16个独立重复；报告行/列平均配对差及描述性平方和分解。复现原151017分布预测，保存全部新轨迹；无DEV/TEST、无调参。

初始速度分布实验：10 TRAIN 拟合视频前半段做逐视频留一初始化器交叉拟合，按视频等权汇总残差中心协方差；不改点估计均值，固定尺度1，只在每条轨迹起点采样一次二维高斯速度。协方差是平滑速度代理残差的经验模型，不是真实后验。原 F、确定性积分 writer、随机初速度积分 writer 在相同 TRAINhold24/P8/300步及新动作seed151017–151020比较；初始化随机数独立于动作流，零协方差须精确复现。无新增 DEV/TEST、不扫尺度、不替换默认模型。

固定writer混合：原F与已拟合prefix积分writer用预定beta=0/.25/.5/.75/1混合；同步速度更新为beta*v_weak+(1-beta)*(2*(y_original-y_current)-v_current)，beta0直接委托原运行时、beta1原积分运行时，避免无效分支溢出破坏端点。相同TRAINhold24/P8/4种子131017–131020筛查综合目标，端点逐项复现已有预测；选中非零模型固定后以8新动作seed141017–141024复核，不扫更细beta、不用DEV/TEST。

前半段重拟合速度记忆writer：复用v19 fit_weak算法但仅传10TRAIN拟合prefix，固定halfwidth8/order3/localpenalty3000；初始化器用因果137维历史特征预测中心9点速度代理相对末帧差分的残差，固定ridge强度N*.001，scale仅拟合数据。平滑前截断prefix。对照原F、记忆+学习初始化、记忆+因果9点初始化、每步因果9点重置、记忆+学习初始化去局部项。相同TRAIN3hold/24窗口/P8/4动作seed131017–131020，保留全部组、不扫参数、不用DEV/TEST；新增运行时失败冻结历史/q/hidden/速度并明确区别原未检查运行时。

v19连续速度接口审计：仅读取旧主候选与当前冻结模型，逐项核对底座/q分区/单位，桥接neural_values特征接口，比较相同TRAIN前半段历史上的旧/桥接初始化与全部F(q,r)一步输出、局部q-pair去除对照。核验原事件读取未变，统计事件条件输出均值差（不是完整事件信息或预测收益）。审计旧训练样本范围，旧权重不作为符合当前10prefix/3hold协议的新模型，不运行新DEV/TEST或训练。

检查点选择审计：固定constant/velocity两类×3优化种子的0/4/8/12检查点；重建模型并核验原第一选择种子62017评分，8新动作seed121017–121024、相同TRAIN留出24窗口/P8/300步复评。报告原选择在新种子上的收益、全8均值最优（仅诊断）、两组4seed交叉选择/评估。全部模型冻结，不依据审计另存部署赢家，不重训、不用DEV/TEST；8seed不是独立新视频验证。

速度条件化两参数writer：原P4/12更新/边界/训练与选模型协议不变，新增delta=value*tanh(last_angle_increment/fit_scale)，静止为零、正负参数均允许；fit_scale仅取10个TRAIN拟合视频300步可用池当前增量绝对值中位数。与恒定偏置同参数数/最大幅度，不保证相同有效RMS。3训练种子全部保留，固定选择结果以新动作seed111017–111020统一TRAIN留出复核，与原constant组和zero比较，不用DEV/TEST。

闭环writer粒子预算训练对照：沿用两参数/边界/优化器/采样器，原P4×12更新为基线；新增P12×4更新（同864000训练粒子步）及P12×12更新（2592000步，同更新次数）。全部3优化种子，每组保持4次TRAIN留出检查含零，4轮组在0/1/2/4检查、12轮组在0/4/8/12检查；更新数减少导致状态覆盖与步长序列变化，显式披露。原默认路径精确重放验证，训练后固定全部候选、新动作seed101017–101020统一留出复核，不挑最好种子、不用DEV/TEST。

等粒子步评分对照：固定上轮9组TRAIN候选/窗口，每组6新动作seed91017–91022、P12/300步；同一批12轨迹分别计算整体U-energy与3个P4子集合U-energy均值，候选间共用随机数，隔离跨子集合配对项。比较配对候选差方差、排名复现与交叉评分；不改模型、不使用留出/DEV/TEST。相同粒子步不等于两种运行实现同CPU；本轮共享轨迹只比较评分，不测独立三次rollout的运行开销。

闭环writer候选排序审计：预先固定3优化种子各第1/6/12轮，共9候选集合；重建原TRAIN拟合窗口、核验原3候选评分，再以6新动作seed81017–81022复评相同候选，P4/300步不变。记录原选择在新种子上相对当前参数的得失、赢家一致率、两组3seed均值排名一致率；不据此改模型、不重新选部署参数、不碰留出/DEV/TEST。

闭环常数校正训练完成后保留全部3优化种子，不挑最好种子；固定各自选定模型，以动作seed72017–72020在相同TRAIN留出视频复核，报告各自与零模型配对差、逐视频结果及条件采样不确定性。此复核不改模型，不是独立视频验证。

直接闭环连续校正训练：先做两参数常数角校正的零初始化有界直接搜索，边界固定为既有真实历史TRAIN拟合99%残差限幅的1%；事件/q/F继续原样运行。每个优化种子1901/2718/3141进行12轮，每轮刷新TRAIN10视频各2窗口、P4、300步，完整评估当前及正负方向3候选（同窗口/随机数），步长预定0.5/sqrt轮次。每4轮在原3TRAIN留出视频24窗口P8/固定2种子选择，零模型始终可选；报告36次训练rollout/种子及覆盖率。此为有噪直接搜索，不称无偏梯度。先不做DEV/TEST，检查原基线与两参数校正的因果性/边界。

固定保护校正器DEV对照：沿用32个DEV尾段窗口、8粒子、动作seed1729/2718/3141，冻结原模型对照逐项复现既有预测；报告逐视频RMSE及U/V energy，不改幅度/模型、不训练、不用TEST。DEV已反复使用，不宣称独立盲测；本轮不接入检查器或搜索。

保护幅度试验后固定模型与幅度，以8个额外TRAIN留出动作种子52017–52024复核；仅验证同视频采样敏感性，不做新数据泛化声明，不依据复核重新调整幅度。

正常状态保护幅度：固定increment模型方向，用10个TRAIN拟合视频真实历史的冻结候选概率，解析求解bounded校正沿单一缩放方向的平均wrapped角残差二次目标最优系数，裁到[0,1]；不使用留出选择幅度。缩放系数和cap一起缩放，不改变原模型文件。3个复用TRAIN留出视频上以新动作seed42017–42020比较零/缩放校正300步自由生成，本轮不用DEV/TEST。保护仅针对拟合平均局部角误差，不是逐状态或闭环保证。

限幅错配审计：固定已保存的observed/absolute/increment校正器，只在TRAIN真实/冻结生成历史上比较无校正、无界线性输出与实际限幅输出，记录概率加权限幅率、原拟合角残差目标、嵌入误差及零幅度处一阶方向。模型不重拟合、不取消部署限幅、不扫幅度，不用DEV/TEST。

运动增量监督对照：沿用生成历史增强的全部状态/权重/归一化/岭强度/真实拟合限幅，仅将生成状态的监督下一帧改为“当前生成角度+真实对应时刻的包裹角增量”。真实历史监督不变；对照绝对位置监督、真实历史训练及零校正，配对同4动作种子TRAIN留出自由生成，不用DEV/TEST。明确这是假设性平移标签，不是真实偏移状态动力学oracle。

生成历史增强校正：固定岭强度1e-4，对照相同起点/目标时刻的真实历史训练与真实+冻结生成历史等权训练；归一化/系数只用10个TRAIN拟合视频，两个模型共用真实历史拟合的限幅，避免混入扩大幅度的改变。3个TRAIN留出视频上以预定4个新动作种子做300步自由生成对照，零模型保留；本轮不做DEV/TEST，不按结果扫幅度。

固定校正方向审计：不拟合参数，只用TRAIN前半段，在真实历史、原模型生成历史及岭校正器自身生成历史的0/8/24/100步快照上，按相同事件/q概率配对比较零/常数/固定岭校正。报告精确局部混合分布energy与MSE改变量、方向一阶项、q矛盾；明确局部收益不等于整段轨迹收益，不用未来真值选择实际生成动作。

连续中心校正试验：TRAIN拟合视频前半段，用冻结路由概率加权所有F(q,r)候选的下一帧残差；比较零校正、常数、因果历史+q/r岭回归。归一化/限幅仅由拟合视频决定；岭强度由TRAIN视频留出单步误差选，随后同一留出集自由生成筛查（不是独立验证）。只有自由生成门槛通过才做预定DEV对照，固定中心原模型保留；不改原F文件，不加入噪声，不把oracle标签送入推理。

同轮固定已选校正器，以8个未用于选择的动作随机种子复核TRAIN留出自由生成评分；不再重拟合、不调阈值。这只验证同一批视频上的采样敏感性，不是新的独立视频验证。

F候选诊断：只用TRAIN前半段，分原拟合/留出视频统计观测历史和自由生成偏移0/24/100步；比较事件概率加权单粒子误差、最佳候选误差、实际集合均值误差，验证偏差/候选方差恒等式。真值只供诊断，oracle不接入推理、不作为长轨迹或集合均值的误差下界；同时标注当前中点检查支持，不能冒充整个后继合法性。

时距错配实验：全部采用300步可用的TRAIN-prefix采样池，留出集统一300步、energy(50/100/300)选择；比较100步×30批、300步×30批、100步×90批/30更新（匹配模拟步数），全部三种子、有/无反馈。默认100步路径不改；显式记录训练模拟步数，区分固定更新数和固定采样成本，说明按步数归一化造成的梯度全局尺度差异。

分布目标学习：同30批/30更新、均匀刷新TRAIN-prefix窗口、三优化种子、同容量有/无冻结评分反馈，比较原逐粒子MSE与energy U目标；TRAIN视频留出按各自目标选择（含零更新），DEV共同评估。能量基线须删去该粒子及所有相关配对项，梯度用穷举小例验证；超参数保持一致但明确损失单位/裁剪差异，不宣称纯粹尺度无关的目标因果效应。

同轮补充 MSE训练/energy选择 对照：与 energy训练/energy选择 使用相同留出选择目标，且核验前者训练更新轨迹与 MSE训练/MSE选择 完全相同，以拆开检查点选择和训练目标改变。

整体计算性价比：普通 CheckedMachine 的32/128/256粒子（原模型及全部三适配器种子），固定DEV32、三轨迹种子，对照已优化8粒子搜索；以实测CPU呈现成本/精度前沿，不谎称严格等预算或相同回退语义。8粒子回归验证、中心检查含末点、仅计算所报时距的精确全粒子energy以控制评估开销；训练与TEST不变。

事件读取缓存对照：固定模型下精确 LRU4096，以模型实例、完整历史/q/hidden 为键；输出只读，模型更新须显式失效。与 fast_probe 比较四模型×三种子的预测、检查及搜索计数；独立交替计时，报告缓存命中率。禁止省略 hidden 或把它当成支持在线学习的通用缓存。

可行性探测提速：新增隔离 fast_probe 路径，只返回首个合法 q 见证，不计算未使用的子节点状态诊断/评分/对象；保持事件读取、候选批次、F 缓存、检查器输入与 RNG 消耗不变。四模型×三轨迹种子与已存 probe_cache 逐位比对，再报告实测 CPU 收益。

提交前可行性探测：浅层步骤先按原评分抽根候选，仅探测该候选是否至少有一个合法后继；窄候选全失败时补全候选，失败则局部排除该 q 并重抽。不得将后继评分混入原评分温度。对比完整每步深度2、原周期5搜索；沿用301内部/300输出的逐点验证、相同回退预算与全部训练种子。

部署策略对照：将均匀刷新训练的无反馈 q-logit 适配器，以可验证清单接入正式检查器和稀疏搜索，三训练种子全部保留。检查器/F/事件抽样顺序不变。新搜索实验生成301步，只返回300步，逐点复核包括末点在内的中心差分检查；原搜索基线需按同协议重跑，不能混用旧300步结果。

状态覆盖对照：固定40个训练前半段窗口 vs 每批重新均匀采样 vs 每个训练视频内部按 q/运动四分位非空格分层抽样。分层阈值仅由10个拟合训练视频的前半段历史计算，预测目标也不得跨过前半段边界。固定30批、30次更新、三训练种子、原留出验证与开发窗口；记录实际覆盖率，不用开发集分布配权。

梯度方差后续：比较默认30次单批更新、10次三批平均更新（同30批采样预算）、30次三批平均更新（同更新次数、90批采样）。三组训练种子和有/无评分反馈都保留；每组7个训练留出检查点，开发集不选参数。各批在同一参数状态下反向传播，平均后统一裁剪并更新。

持续优化目标下的下一轮：在隔离脚本中用自由生成轨迹的多步误差训练残差转移适配器（原 GRU/F 冻结），与同容量评分反馈版本对照。只用训练视频前半段；训练视频级留出选择，开发集最终评估。原始模型作为零更新候选，不强制部署新模型。

2026-09-17 新增：用户要求试验将已选事件/q 的评分或置信度反馈进后续模型，以及以现有离散候选点为中心的连续分布。先做独立消融，再考虑组合；训练限训练视频前半段，保留训练视频级验证，开发集评估，冻结包不变。默认先测试 F 输出角度点作为分布中心；q 聚类中心的连续潜变量是另一种接口，已向用户发出可选澄清问题。

用户已授权实现并试验隔离的可变深度搜索原型、优化运行速度，以及修正检查器接口后诊断约束/候选覆盖/价值评分。可训练隔离的价值模型，训练只用训练视频，开发集用于对照；保留原版本包不变。当前运行规模不需要强行使用 120 线程。

## 目标

当前长期目标：持续实证探索该预测任务的优化路径（用户原文：consistently explore the optimiazatoin pathway for this task）。版本阅读已形成基础记录；当前以可追溯实验、等预算对照、视频级泛化检查与冻结默认行为为工作准则。完成一次实验不等于长期目标完成。

按 v17 → v18 → v20 的顺序，阅读工作区中的完整版本包，形成可追溯、可持续更新的项目理解，并把关键知识维护在 `.agents/` 中，供后续会话复用。

## 工作范围

- 阅读协议、README、报告、核心运行时代码、训练/评估脚本、冻结记录和关键结果。
- 分清训练、开发选择、冻结测试与事后解释，避免数据泄漏式结论。
- 比较相邻版本的目标、实现、指标、成功项、失败项和遗留问题。
- 校验 ZIP 完整性、文件布局和关键哈希；必要时运行包内自检。
- 每完成一个版本，在 `.agents/versions/` 写详细记录，并同步更新 `.agents/MEMORY.md`。

## 默认不做

- 不重新训练或重新选择模型。
- 不修改版本包中的源码、模型或结果。
- 不把相关窗口当成独立重复实验，不把复用测试集称为全新盲测。
- 不猜测缺失的 v19 ZIP 原因；只有从后续包内证据确认后才记录。

## 交付顺序

1. v17：第一轮阅读已完成，后续版本比较时可补充。
2. v18：下一步阅读与总结。
3. v20：最后阅读；同时识别其内含的 v19 过渡材料。
4. 汇总跨版本演进、当前最佳证据、未解决风险与建议的下一步。

## 协作约定

- 面向用户的回复始终使用中文。
- 先给结论，再给必要证据；重要断言指向具体文件。
- 若任务范围发生变化，先更新本文件，再继续执行。
全TRAIN软门控双区验证已完成（2026-09-18）：前段丰富门控改善1.296%，后段仍退化1.843%；不能推广。全部精度门槛、173测试通过，旧失败保留。详细见 notes/full_soft_gate_holdout.md。下一候选是拟合侧10视频的时间覆盖审计与时间块内部验证，再考虑因果不确定性特征/联合alpha核宽；尚未执行，不用历史hold调参。
